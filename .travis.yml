language: swift
osx_image: xcode10.1
branches:
  only:
    - xcuitest_poc
before_install:
  - gem install fastlane --no-document
install: true
script:
  - fastlane scan --scheme JellyfinPlayer --device "iPhone XR"
